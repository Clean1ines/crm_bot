Ты — Implementation Engineer для crm_bot. Ветка: codex/implement-knowledge-document-upload-pipeline Режим: /implementation Обязательные документы: - docs/codex_propmts/preamble.md - docs/codex_propmts/codex_realisation.md - docs/architecture/Knowledge Document_Upload_Pipeline_Contract_v1.md - docs/architecture/application_contract_audit_v1.md Сначала read-only recon: 1. Прочитай документы выше. 2. Выпиши применимые инварианты. 3. Выпиши forbidden behavior. 4. Выпиши список файлов, которые будешь менять. 5. Не меняй код до этого. Задача: Довести ветку codex/implement-knowledge-document-upload-pipeline до реального Knowledge Document Upload Pipeline Contract v1. Текущие блокеры, которые нужно исправить: 1. Fake resume: src/infrastructure/queue/handlers/knowledge_resume_processing.py сейчас вызывает: service.publish_ready_answers(...) Это запрещено. Resume is not publish-ready. 2. Ветка не реализует настоящий resume в KnowledgeIngestionService: resume_processing должен идти через normal answer resolution / publication path: answer_resolution_pending → answer resolution / merge → canonical entries → publication → embeddings → retrieval surface → processed 3. Frontend API не адаптирован к backend command contract: backend endpoints требуют expected_state и expected_state_version, но frontend/src/shared/api/modules/knowledge.ts отправляет POST без body. 4. Frontend action renderer не понимает: - resume_processing - publish_raw_drafts_without_resolution и всё ещё ищет старый action.id === 'publish_ready'. 5. Tests сейчас недостаточны: они проверяют наличие строк/dispatch, но не ловят fake resume. Некоторые service tests ожидают старый payload и старые states. Non-negotiable invariants: 1. Raw drafts are not knowledge. 2. Retry failed batches only retries extraction/compiler batches. 3. Retry failed batches never publishes. 4. Retry failed batches never builds embeddings. 5. Retry failed batches never marks document processed. 6. Resume is not publish-ready. 7. Resume must use shared post-extraction answer resolution/publication pipeline. 8. Resume must never call publish_ready_answers / publish raw drafts fallback. 9. Fallback publish must be explicitly labelled as without resolution. 10. Processed means retrieval surface is truly ready. 11. Frontend actions must come from backend allowed_actions. 12. Queue task types must be known, dispatched, handled and tested. 13. No user-visible raw provider payload. 14. No frontend button without working backend path. Forbidden behavior: - handle_resume_knowledge_processing → publish_ready_answers - resume_processing → publish_ready_answers - retry_failed_batches → _persist_stage_e_compiler_outputs - retry_failed_batches → embeddings - retry_failed_batches → document.status processed - endpoint without task/handler/service/test - task type without KNOWN_TASK_TYPES + dispatcher + handler + test - frontend local guessing instead of backend allowed_actions - NotImplemented in user-visible path - tests that only search for strings and do not verify behavior Required backend changes: 1. Implement true KnowledgeIngestionService.resume_processing if missing. 2. It must validate: - document belongs to project; - stage is answer_resolution_pending or explicitly allowed extraction_completed; - all compiler batches completed; - no failed/pending/processing batches; - raw candidates exist; - source chunks exist; - compiler_run_id is unambiguous. 3. It must call the shared post-extraction answer resolution/publication pipeline. 4. It must not call publish_ready_answers. 5. Update knowledge_resume_processing handler to call true resume_processing and pass all required dependencies, including preprocessor_factory if required. 6. Handler must map: - ValidationError / KnowledgePreprocessingValidationError → PermanentJobError - retryable provider/embedding errors → TransientJobError if such typed errors exist - non-retryable provider/embedding errors → PermanentJobError Required frontend changes: 1. Update KnowledgeProcessingReport type: - state - state_version - state_hash - recommended_next_action - active_error - last_error 2. Update knowledgeApi command methods to send: { expected_state, expected_state_version } 3. Add resumeProcessing API method if missing. 4. Update KnowledgePage action renderer: - use processingReport.actions from backend; - handle retry_failed_batches; - handle resume_processing; - handle publish_raw_drafts_without_resolution; - handle cancel; - handle retighten if backend returns it. 5. Remove hardcoded publish_ready action check or map it only as legacy fallback if backend still returns it. 6. Do not render enabled action as passive span when it has known command behavior. 7. Disable conflicting actions while mutation pending. Required tests: 1. resume_handler_does_not_call_publish_ready_answers. 2. resume_handler_calls_true_resume_processing. 3. resume_processing_uses_shared_post_extraction_pipeline. 4. resume_processing_rejects_failed_batches. 5. resume_processing_rejects_pending_or_processing_batches. 6. retry_failed_batches_does_not_publish_embed_or_mark_processed. 7. frontend/api sends expected_state and expected_state_version for command mutations, if frontend test infra exists. 8. processing_report answer_resolution_pending returns: - resume_processing - publish_raw_drafts_without_resolution - no retry - no cancel if no active job. 9. queue registry test proves task known + dispatched + handler behavior. 10. no test should only assert that strings exist if behavior can be tested. Validation: Backend: - venv/bin/python -m ruff format --check src tests - venv/bin/python -m ruff check src tests - venv/bin/python -m mypy src - venv/bin/python -m pytest tests/domain/test_knowledge_document_pipeline.py - venv/bin/python -m pytest tests/architecture/test_knowledge_pipeline_contract_guards.py tests/architecture/test_knowledge_pipeline_queue_registry_contract.py tests/architecture/test_knowledge_pipeline_stage_literals_guard.py - venv/bin/python -m pytest tests/application/services/test_knowledge_service_runtime.py Frontend: - npm --prefix frontend run lint - npm --prefix frontend run type-check - npm --prefix frontend run build Before final response: Run self-adversarial review: 1. Is there user-visible action without working backend path? 2. Does resume call publish_ready anywhere? 3. Does retry publish/embed/processed anywhere? 4. Does frontend send expected_state/version? 5. Are task types known and dispatched? 6. Are tests strong enough to catch fake resume? Final response must include Contract Compliance Table: Invariant | Evidence | Test PROMPT 