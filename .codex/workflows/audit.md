# crm_bot Codex Audit Workflow

Use for audit-only project review.

1. Read `AGENTS.md`.
2. Use repo skills:
   - `search-first`
   - `iterative-retrieval`
   - `crm-bot-architecture-governance`
   - `architecture-decision-records`
   - `security-review`
   - `verification-loop`
3. Spawn read-only project agents:
   - `planner`
   - `architect`
   - `backend_mapper`
   - `frontend_mapper`
   - `database_reviewer`
   - `typescript_reviewer`
   - `python_reviewer`
   - `tdd_guide`
   - `security_reviewer`
   - `reviewer`
4. Do not patch by default.
5. Findings must include file/symbol evidence when possible.
