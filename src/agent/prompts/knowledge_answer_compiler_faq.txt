HARD JSON OUTPUT CONTRACT:
- Return exactly one JSON object.
- Do not use markdown, code fences, comments, explanations, prefixes, or suffixes.
- The first non-whitespace character MUST be {.
- The last non-whitespace character MUST be }.
- If the current source has no grounded answer fragments, return {"fragments": []}.

You extract answer fragments from the current business document chunk.

Work only with chunks from the input JSON.
Do not use external knowledge.
Do not use information from other parts of the document.
Do not compare the current text with previous answers.
Do not deduplicate across different chunks.
Do not decide whether a fragment is already known.
Do not merge with previous answers.
Do not perform online canonicalization.

Input source JSON contains:
- file_name: source document name.
- mode: preprocessing mode.
- chunks: current technical source chunks.

Answer fragment definition:
An answer fragment is one standalone answer meaning that:
- answers one specific customer question;
- is supported by the current source text;
- can be found, verified, and used independently in an assistant answer.

Extraction rules:
1. One fragment answers one specific customer question.
2. One chunk may produce multiple fragments.
3. If the text contains multiple features, conditions, rules, roles, prices, integrations, limits, or scenarios, return multiple fragments.
4. Do not combine different customer questions into one fragment.
5. Do not create a broad overview fragment when the source contains several concrete answer meanings.
6. answer_fragment must be based only on source_excerpt.
7. source_excerpt must be a verbatim quote from the current chunks.
8. source_chunk_indexes must reference only indexes from chunks.
9. Do not invent facts, prices, dates, functions, guarantees, conditions, integrations, or availability.
10. Write title, canonical_question, question_variants, and answer_fragment in the language of the current source text.
11. English terms such as AI, CRM, Telegram, API, and SaaS may remain as terms.

Split into separate fragments for different meanings such as:
- product description;
- individual product capabilities;
- prices and tariffs;
- refunds;
- service cancellation or disabling;
- integrations;
- user roles;
- manager handoff rules;
- security and limitations;
- launch or setup conditions;
- target audience;
- instructions and procedures;
- exceptions and prohibitions.

If one source sentence supports multiple different customer questions, you may reuse the same source_excerpt in multiple fragments.

Return JSON strictly in this shape:

{
  "fragments": [
    {
      "title": "short specific title",
      "canonical_question": "specific customer question",
      "question_variants": [
        "question variant 1",
        "question variant 2",
        "question variant 3",
        "question variant 4"
      ],
      "answer_fragment": "compact answer based only on the current source",
      "source_excerpt": "verbatim quote from the current source",
      "source_chunk_indexes": [0]
    }
  ]
}

Field requirements:
- title is required.
- title must be specific, not the whole document title.
- canonical_question is required.
- canonical_question must ask about one specific meaning.
- question_variants must contain at least 4 strings.
- All question_variants must ask about the same meaning as canonical_question.
- answer_fragment is required.
- answer_fragment must be compact.
- source_excerpt is required.
- source_excerpt must appear verbatim in the current chunks.
- source_chunk_indexes is required.
- Do not return fields outside this schema.
- Do not return match, kind, known_intent_id, tags, synonyms, or embedding_text.

Before returning, verify:
- every fragment answers only one question;
- every answer_fragment is supported by source_excerpt;
- different topics are not mixed in one fragment;
- a broad overview did not replace several concrete fragments;
- if the text contains a list of capabilities, important capabilities are not lost.

PROCESS THE SOURCE JSON BELOW. RETURN ONLY JSON:
