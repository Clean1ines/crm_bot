import React, { useState } from 'react';
import type { RagEvalReviewGroup, RagEvalReviewQuestion } from '@shared/api/modules/ragEval';
import { ReportJsonBlock } from './RagEvalReportComponents';
import { asStringList } from '../lib/ragEvalResults';

type RagEvalReviewRetrievedEntryItem = RagEvalReviewQuestion['retrieved_entries'][number];
const asRetrievedEntries = (value: unknown): RagEvalReviewRetrievedEntryItem[] => (Array.isArray(value) ? value.filter((item): item is RagEvalReviewRetrievedEntryItem => Boolean(item) && typeof item === 'object') : []);

export const QuestionReviewDrawer: React.FC<{ question: RagEvalReviewQuestion | null; group: RagEvalReviewGroup | null; onClose: () => void; onAccept: (questionId: string) => void; onReject: (questionId: string) => void; onEdit: (questionId: string, value: string) => void; mutating: boolean; }> = ({ question, group, onClose, onAccept, onReject, onEdit, mutating }) => {
  const [editValue, setEditValue] = useState('');
  if (!question || !group) return null;
  const currentEditValue = editValue || question.effective_question;
  const retrievedEntries = asRetrievedEntries(question.retrieved_entries);
  const proposedImprovements = asStringList(question.proposed_improvements);
  return <div className="fixed inset-0 z-50 flex justify-end bg-black/30" onClick={onClose}><aside className="h-full w-full max-w-xl overflow-auto bg-[var(--surface-elevated)] p-5 shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-medium text-[var(--accent-primary)]">Вопрос-кандидат</p><h3 className="mt-2 text-xl font-semibold text-[var(--text-primary)]">“{question.effective_question}”</h3></div><button type="button" onClick={onClose} className="rounded-lg border border-[var(--border-primary)] px-3 py-1 text-sm text-[var(--text-primary)]">Закрыть</button></div><div className="mt-5 space-y-4"><div className="rounded-xl bg-[var(--control-bg)] p-4 text-sm text-[var(--text-secondary)]"><div>Тип: <span className="font-semibold text-[var(--text-primary)]">{question.question_type_label}</span></div><div className="mt-1">Статус: <span className="font-semibold text-[var(--text-primary)]">{question.retrieval_status_label}</span></div><div className="mt-1">Review state: <span className="font-semibold text-[var(--text-primary)]">{question.review.status}</span></div></div><section><h4 className="text-sm font-semibold text-[var(--text-primary)]">Ожидался фрагмент</h4><p className="mt-2 rounded-xl bg-[var(--control-bg)] p-3 text-sm leading-6 text-[var(--text-secondary)]">{group.content || group.title}</p></section><section><h4 className="text-sm font-semibold text-[var(--text-primary)]">Что нашёл поиск</h4><ol className="mt-2 space-y-2 text-sm text-[var(--text-secondary)]">{retrievedEntries.map((entry, index) => <li key={`${entry.id}-${index}`} className="rounded-xl bg-[var(--control-bg)] p-3">{index + 1}. {entry.title || entry.id}<p className="mt-1 line-clamp-2 text-xs text-[var(--text-muted)]">{entry.content}</p></li>)}{!retrievedEntries.length && <li className="rounded-xl bg-[var(--control-bg)] p-3">Поиск не вернул фрагменты.</li>}</ol></section><section><h4 className="text-sm font-semibold text-[var(--text-primary)]">Почему это проблема</h4><p className="mt-2 text-sm leading-6 text-[var(--text-secondary)]">{question.why_it_matters}</p></section><section><h4 className="text-sm font-semibold text-[var(--text-primary)]">Что можно сделать</h4><ul className="mt-2 space-y-1 text-sm text-[var(--text-secondary)]">{proposedImprovements.map((item: string) => <li key={item}>☑ {item}</li>)}<li>☐ Отклонить как плохой вопрос</li></ul></section><section><h4 className="text-sm font-semibold text-[var(--text-primary)]">Редактировать формулировку</h4><textarea value={currentEditValue} onChange={(event) => setEditValue(event.target.value)} className="mt-2 min-h-24 w-full rounded-xl border border-[var(--border-primary)] bg-[var(--control-bg)] p-3 text-sm text-[var(--text-primary)] outline-none" /></section><div className="flex flex-wrap gap-2"><button type="button" disabled={mutating} onClick={() => onAccept(question.question_id)} className="rounded-xl bg-[var(--accent-primary)] px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">Принять</button><button type="button" disabled={mutating} onClick={() => onEdit(question.question_id, currentEditValue)} className="rounded-xl border border-[var(--border-primary)] px-4 py-2 text-sm font-semibold text-[var(--text-primary)] disabled:opacity-50">Сохранить редакцию</button><button type="button" disabled={mutating} onClick={() => onReject(question.question_id)} className="rounded-xl border border-red-500/30 px-4 py-2 text-sm font-semibold text-red-500 disabled:opacity-50">Отклонить</button></div><details className="rounded-xl border border-[var(--border-primary)] p-3"><summary className="cursor-pointer text-sm font-medium text-[var(--text-primary)]">Диагностика</summary><div className="mt-3"><ReportJsonBlock value={question.diagnostics} /></div></details></div></aside></div>;
};
