# PR #22 Final Scope & Validation Report

## Updated PR body (ready to publish)

### Motivation
- Reduce complexity of `KnowledgePage.tsx` by extracting large document-related UI into page-local components.
- Prepare the document card UX for compact operation with primary actions and expandable details.
- Improve i18n correctness for document-card action labels and counters.

### Actual Scope (honest summary)
- Extracted commercial truth summary (`CommercialTruthReviewSummary`).
- Extracted price facts summary (`PriceFactsSummary`).
- Extracted drafts/source units summaries and modals (`DraftsSummary`, `DraftsModal`, `SourceUnitsSummary`, `SourceUnitsModal`).
- Introduced document card components (`KnowledgeDocumentCard`, `DocumentActionsBlock`, `DocumentProcessingBlock`, `DocumentStatusBlock`).
- Introduced collapsible document details panel (`KnowledgeDocumentDetailsPanel`, renamed from actions menu naming).
- Localized document card primary actions and details title via i18n keys.
- Added compact first-level counters on document card (chunks, drafts, price facts, commercial conflicts) when data is available.
- Extracted `ImportQualitySummary` into page-local component and removed component injection from document card.
- Removed summary component injection for `PriceFactsSummary` and `CommercialTruthReviewSummary` from document card.

### What is NOT done
- Backend/API changes: **not done** (unchanged).
- New dependencies: **not added**.
- WorkspaceSummary: **not implemented**.
- ReviewInbox: **not implemented**.
- DebugMode: **not implemented**.

### Validation
- `npm run lint`: **PASS**.
- `npm run type-check`: **PASS**.

### Risks / review notes
- Document card behavior/layout changed from pure extraction into compact-card + details panel behavior.
- Manual visual smoke review is recommended for document-card states/actions.

### Suggested next PRs
1. WorkspaceSummary
2. ReviewInbox
3. DebugMode

## gh CLI update status
- If `gh` is available and authenticated, use this report body to update PR description.
- If unavailable, this file is the source of truth for updated PR body text.
