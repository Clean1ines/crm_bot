[pytest]
addopts = -v --tb=short --strict-markers
testpaths = tests
pythonpath = .
markers =
    integration: marks tests as integration (deselect with '-m "not integration"')
    anyio: marks async tests run via anyio plugin
    slow: marks tests as slow (deselect with '-m "not slow"')
